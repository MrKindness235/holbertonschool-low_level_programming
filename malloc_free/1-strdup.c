#include "main.h"

/**
 * *_strdup - Return a pointer to a new string which is
 *
 * Return: pointer.
 *
 * @str: given string.
 */

char *_strdup(char *str)
{
	char *p;

	if (str == NULL)
	{
		return (NULL);
	}
	if (!str)
	{
		return (NULL);
	}
	p = str;
	return (p);
}

#include "main.h"

/**
 * print_alphabet -Prints the alphabet in lowercase followed by a line
 *
 * Return: zero
 */

void print_alphabet(void)
{
int a[] = {97} ... {122};
int i;

for (i = 0; i < 25; i++)
{
_putchar (a[i]);
}
_putchar('\n');
return (0);
}
